apiVersion: v1
kind: Service
metadata:
  name: flaskapp-service
  namespace: flask-production
spec:
  type: NodePort
  selector:
    app: flaskapp
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30000
